{
  "rules": [
    {
      "rule_id": "R1",
      "type": "arithmetic",
      "description": "The enrollment fee must satisfy multiple conditions relative to the catalog fee and attendance percentage",
      "condition": "(enrollment.fee - catalog.fee) == 1700 and enrollment.fee % 250 == 0 and 1.0 < (enrollment.fee / catalog.fee) <= 1.5 and 75 <= exam.min_attendance_pct <= 100 and (exam.min_attendance_pct * enrollment.fee / 100) >= (catalog.fee - 200)",
      "severity": "HIGH",
      "on_failure": "Adjust the enrollment fee to meet the specified conditions",
      "data_sources": [
        "catalog",
        "enrollment",
        "exam"
      ]
    },
    {
      "rule_id": "R2",
      "type": "english",
      "description": "Student qualifies for the exam based on course name and attendance or fee clearance and eligibility",
      "condition": "Check whether the course name contains 'Basics' or 'Intro' and the student's attendance percentage is 75 or higher, or if the course name does not contain 'Basics' or 'Intro', then the student must have cleared the exam fee and be marked as eligible",
      "severity": "HIGH",
      "on_failure": "The student does not qualify for the exam due to insufficient attendance or uncleared exam fee or ineligibility",
      "data_sources": [
        "catalog",
        "enrollment",
        "exam"
      ]
    },
    {
      "rule_id": "R3",
      "type": "sql",
      "description": "Each course must have exactly two students enrolled.",
      "condition": "SELECT CASE WHEN COUNT(DISTINCT student_name) = 2 THEN 1 ELSE 0 END FROM ENROLL_USER.ENROLLMENT WHERE course_id = :course_id",
      "severity": "HIGH",
      "on_failure": "Enroll one more student or remove one student to meet the two-student requirement.",
      "data_sources": [
        "enrollment"
      ]
    }
  ]
}